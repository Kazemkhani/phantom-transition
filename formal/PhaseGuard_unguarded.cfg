\* The fault: Session(preemptive_generation=True,
\* cancel_handoff_on_interrupt=False, guard_enabled=False), the configuration
\* of test_phantom_transition_reproduces. TLC reports the first violation it
\* reaches in breadth-first order and prints the trace.
SPECIFICATION Spec
CONSTANTS
    PreemptiveGeneration = TRUE
    CancelOnInterrupt = FALSE
    GuardEnabled = FALSE
    MaxTurns = 4
    Forged = Forged
INVARIANTS
    TypeOK
    NoBackwardMove
    AtMostOneStep
    InterruptedTurnsAreInert
    EntryConditionRespected
    GroundedTransition
