\* The same unguarded session, checking only the interruption invariant, so
\* the trace TLC prints is the phantom transition itself (Figure 1 of the
\* paper): a handoff that executed on a turn the caller then invalidated.
SPECIFICATION Spec
CONSTANTS
    PreemptiveGeneration = TRUE
    CancelOnInterrupt = FALSE
    GuardEnabled = FALSE
    MaxTurns = 4
    Forged = Forged
INVARIANTS
    InterruptedTurnsAreInert
